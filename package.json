{
  "name": "eleicoes-2026-transparencia",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/prepare-public-data.mjs && node scripts/build_candidate_seo.mjs",
    "build": "vite build",
    "postbuild": "node scripts/check_navigation_build.mjs && node scripts/check_candidate_seo.mjs && node scripts/smoke_preview.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
